---
id: "ISSUE-20260624-534f"
title: "Angular templates: getters accessed without (), signal inputs require ()"
first_seen: "2026-06-24"
last_seen: "2026-06-24"
occurrences: 2
sessions: ["68e8a9c8-2a81-4156-a433-f7aae98d7115", "68e8a9c8-2a81-4156-a433-f7aae98d7115"]
category: "angular"
status: "open"
certainty: "medium"
---

## Description

In Angular component templates, TypeScript class getters are accessed **without** parentheses (e.g., `bullishPatterns.length`), while `input()` signal properties require `()` to read their value (e.g., `patterns().length`). Confusing the two causes template compilation errors.

Error: `TS6234: This expression is not callable because it is a 'get' accessor. Did you mean to use it without '()'?`

## Evidence

- Session 68e8a9c8: In `pattern-overlay.html`, getters `bullishPatterns()`, `bearishPatterns()`, `neutralPatterns()` were called with `()` causing TS6234. Fixed by removing `()` from getter accesses.
- Session 68e8a9c8 (Phase 4): In `analysis-dashboard.html`, getters `isAnalyzing()`, `isConfigured()`, `analysis` were called with `()` causing TS6234. Also `isOpen` is a `signal()` (needs `()`) but was used without. Fixed by distinguishing: getters no `()`, signals yes `()`.

## Decision

**Promoted** 2026-06-27 → `/memories/angular-pitfalls.md`.
Occurred twice across sessions. Promoted alongside the standalone-pipes issue
into a single `angular-pitfalls.md` memory file.
